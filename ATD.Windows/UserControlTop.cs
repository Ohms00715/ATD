using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Drawing;
using System.Data;
using System.Linq;
using System.Text;
using System.Windows.Forms;


namespace ATD.Windows
{
    public partial class UserControlTop : UserControl
    {
        public UserControlTop()
        {
            InitializeComponent();
        }

        private void BTEMP_Click(object sender, EventArgs e)
        {
            ////MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            ////if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS100");

            //}

        }

        private void BTTitle_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS200");

            //}
        }

        private void BTDep_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS300");

            //}
        }

        private void BTType_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS400");

            //}
        }

        private void BTtime_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS500");

            //}
        }

        private void BTDay_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS600");

            //}
        }

        private void BTVacation_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS700");

            //}
        }

        private void BTTeam_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("MAS800");

            //}
        }

        private void BTImport_Click(object sender, EventArgs e)
        {
            //MDIForm fMdi = (MDIForm)this.ParentForm.MdiParent;
            //if (fMdi != null)
            //{
            //    fMdi.callMdiChild("TRN100");

            //}
        }
        }

     
    }

